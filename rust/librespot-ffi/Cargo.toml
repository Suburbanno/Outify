[package]
name = "librespot-ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
librespot-api = { path = "../librespot-api" }
uniffi = { version = "0.27", features = ["tokio"] }
